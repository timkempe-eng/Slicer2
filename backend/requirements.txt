fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

# Durable stack: persistent jobs (Postgres), task queue (Redis/RQ), object
# storage (Spaces/S3). The app falls back to sqlite + local disk when these are
# unconfigured, so dev/test need no external services.
SQLAlchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13
rq>=1.16
redis>=5.0
boto3>=1.34

# Printer delivery (used by app/printer/*). paho is for MQTT start-print.
# Auto-push is out of MVP scope; kept for the future on-network "local bridge".
paho-mqtt>=2.0
